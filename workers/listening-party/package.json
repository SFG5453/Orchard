{
  "name": "@orchard/listening-party-worker",
  "version": "0.1.0",
  "description": "Peer-to-peer listening party signaling service for Orchard.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types --check && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local",
    "test": "node --test",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
